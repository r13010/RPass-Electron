function InterfaceShowLoading(x) {
    /*
    const pageloading = document.querySelector("#pageloading");
    if (x == 1) {
        pageloading.style.opacity = "1";
    } else if (x == 0) {
        pageloading.style.opacity = "0";
    }
    */
    document.querySelector("#pageloading").style.opacity = "1";
}